# Privacy Model

- Local-only mode is default.
- Connector calls are optional and explicitly controlled.
- Every enrichment invocation records `external_compute_used` audit metadata.
- No biometric identity recognition is allowed.
- Cluster labels are user-assigned metadata only.
